using System; // Imports the System namespace, which contains basic classes like Console.

class Program // Defines a class named Program. In C#, all code must be inside a class.
{
    static void Main() // The entry point of the program. Execution starts here.
    {
        Console.WriteLine("Hello, World!"); // Prints text to the console followed by a newline.
    }
}
